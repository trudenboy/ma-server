name: Upstream API Change
description: Report a breaking change in the upstream API used by the ma_server provider
labels: ["incident:upstream", "priority:high"]
body:
  - type: markdown
    attributes:
      value: |
        Use this template to report changes in the upstream service API that affect the provider.

  - type: input
    id: upstream_ref
    attributes:
      label: Upstream repo / commit / PR
      description: Link to the upstream change (commit, PR, changelog entry, or announcement).
      placeholder: "e.g. https://github.com/upstream/repo/pull/123"
    validations:
      required: true

  - type: textarea
    id: what_changed
    attributes:
      label: What changed upstream
      description: Describe the API change — new endpoints, removed fields, auth changes, etc.
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: Impact on ma_server provider
      description: Which provider features are broken or affected?
    validations:
      required: true

  - type: textarea
    id: proposed_fix
    attributes:
      label: Proposed fix (optional)
      description: If you have ideas for how to adapt the provider, describe them here.

name: Bug Report
description: Report a bug in the ma_server provider
labels: ["incident:bug", "priority:medium"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to report a bug! Please fill in as much detail as possible.

  - type: input
    id: provider_version
    attributes:
      label: Provider version
      description: |
        Find this in the `VERSION` file at the repo root, or in the latest GitHub Release tag.
      placeholder: "e.g. 2.1.0"
    validations:
      required: true

  - type: input
    id: ma_version
    attributes:
      label: Music Assistant version
      placeholder: "e.g. 2.3.1"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. Go to ...
        2. Click on ...
        3. See error
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behaviour
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual behaviour
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Relevant logs
      description: Paste any relevant log output here.
      render: text

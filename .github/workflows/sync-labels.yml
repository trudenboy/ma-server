name: Sync Labels
on:
  push:
    branches: [main, dev]
    paths:
      - .github/labels.yml
  workflow_dispatch:

jobs:
  sync-labels:
    uses: trudenboy/ma-provider-tools/.github/workflows/reusable-sync-labels.yml@main
    secrets: inherit
